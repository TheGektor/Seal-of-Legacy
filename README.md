# Seal-of-Legacy

![Java](https://img.shields.io/badge/Java-21-orange) ![Paper](https://img.shields.io/badge/Paper-1.21.1-blue) ![License](https://img.shields.io/badge/License-Proprietary-red)

**Seal-of-Legacy** — это продвинутый плагин для Minecraft серверов (Paper/Spigot 1.21+), который добавляет механику "запечатывания" уникальных предметов. Запечатанные предметы защищены от копирования, изменения и использования в рецептах, что позволяет создавать уникальные артефакты для экономики сервера.

## 🚀 Возможности

- **🔒 Надежная защита**:
  - Блокировка копирования книг и карт.
  - Запрет на использование в крафтах (Верстак).
  - Защита от изменений на Наковальне (переименование, добавление чар/тримов).
  - Защита от снятия чар/печати на Точиле.
- **💾 База данных**: Все данные о печатях (UUID, Владелец, Дата) хранятся в локальной `SQLite` базе данных.
- **🎨 Современный дизайн**: Использование Adventure API и MiniMessage для красивых градиентов и текстов.
- **🛡️ NBT Идентификация**: Предметы помечаются уникальным NBT тегом для однозначной идентификации.

## 📥 Установка

1. Скачайте последний релиз `.jar` файла.
2. Убедитесь, что ваш сервер работает на **Paper 1.21.1** (или выше) и **Java 21**.
3. Поместите файл в папку `plugins/`.
4. Перезапустите сервер.

## 🛠️ Команды и Права

| Команда | Описание | Право (Permission) |
|---|---|---|
| `/lockitem` | Запечатать предмет в руке. | `itemlock.use` |
| `/unlockitem` | Снять печать (только владелец). | `itemlock.unlock` |
| `/lockcheck` | Проверить, запечатан ли предмет. | `itemlock.check` |
| `/lockinfo` | Информация о печати (ID, Владелец, Дата). | `itemlock.info` |
| **Административные** | | `itemlock.admin` |
| `/lockitem force` | Принудительно запечатать предмет (игнорируя владельца). | |
| `/lockitem list` | Показать список запечатанных предметов (в разработке). | |
| `/lockitem purge` | Очистить базу данных от всех записей. | |
| `/lockreload` | Перезагрузить конфигурацию `.yml`. | |

> **Примечание:** Операторы (OP) по умолчанию имеют право `itemlock.bypass`, позволяющее игнорировать защиту.

## ⚙️ Конфигурация

Файл `config.yml` позволяет настроить сообщения и визуальный стиль.

```yaml
messages:
  prefix: "<gradient:gold:yellow>[SealOfLegacy]</gradient> <dark_gray>» <gray>"
  sealed-success: "<green>Предмет успешно запечатан!"
  # ... другие сообщения

settings:
  lore-line: "<gradient:red:dark_red>🔒 ЗАПЕЧАТАНО</gradient>" # Строка, добавляемая в лор
```

## 🤝 Участие в разработке (Contributing)

Мы приветствуем вклад в развитие проекта! Если вы хотите помочь, следуйте этим шагам:

### Требования
- JDK 21
- Gradle (используется Wrapper в проекте)

### Как начать
1. **Форкните репозиторий** и клонируйте его к себе.
2. Откройте проект в вашей IDE (IntelliJ IDEA рекомендуем).
3. Убедитесь, что проект успешно собирается:
   ```bash
   ./gradlew build
   ```
4. Для тестирования используйте встроенную задачу запуска сервера:
   ```bash
   ./gradlew runServer
   ```
   *При первом запуске сервер скачается автоматически.*

### Структура проекта
Мы придерживаемся архитектуры **Package by Feature**:
- `features.sealing` — Логика наложения печати, DAO базы данных.
- `features.protection` — Слушатели событий (Listeners) для защиты.
- `features.commands` — Обработчики команд.
- `database` — Работа с SQLite.
- `utils` — Утилиты (MiniMessage и т.д.).

### Правила Code Style
- Используйте **Adventure API** для текста (никаких `§` или `ChatColor`).
- Весь IO и работа с БД должны быть **асинхронными** (CompletableFuture).
- Следуйте принципам **Clean Code** и **SOLID**.

### Процесс Pull Request (PR)
1. Создайте ветку для вашей фичи: `git checkout -b feature/my-cool-feature`.
2. Внесите изменения и протестируйте их.
3. Закоммитьте изменения с понятным сообщением.
4. Отправьте ветку (Push) и создайте Pull Request в основной репозиторий.

---
**Автор:** Antag0nis1
**Версия:** 1.0.0